spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8769
      #      fail-fast: true
      #      name: config-client
      profile: dev
server:
  port: 8762
